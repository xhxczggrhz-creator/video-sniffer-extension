# 隐私政策（Privacy Policy）

## 一句话承诺

**视频嗅探器完全在本地处理，不收集、不存储、不出网传输任何与你浏览行为相关的数据。**

## 本地处理边界

| 类型 | 处理方式 |
|---|---|
| 你在网页上看的 URL / 视频内容 | **绝不离开你的浏览器**。嗅探、MSE 拦截、下载、音视频合并全部在本机完成。 |
| 下载的视频数据 | 仅在你主动点击"导出"时写入本地磁盘；不落盘任何云端、无遥测。 |
| 防盗链签名、Cookie、DRM 相关凭据 | 仅在浏览器会话内存（`chrome.storage.session`）内使用，读取后立即清除，**不写入** `storage.local`（避免被导出/持久化），**不进入** URL query / 浏览器历史。 |
| 第三方运行时依赖 | 全部为本地静态资源（如 mp4box、mux.js），运行时不额外请求任何 CDN。 |

## 网络行为
- 扩展发出的所有请求均为**你主动触发下载时对目标视频站点的直连请求**。
- **无任何遥测、无统计 SDK、无广告、无第三方分析脚本。**
- 权限 `webRequest` 仅用于读取目标媒体响应并拼接下载，不用于数据采集。

## 权限按需使用
本扩展申请的网络相关权限用于完成"嗅探–代理–下载"这一核心功能。若未来加入任何遥测或
统计能力，本文件必须同步披露后才能上线。

## 卸载
卸载扩展不会在你机外留下任何数据；本地下载文件由你自行管理删除。